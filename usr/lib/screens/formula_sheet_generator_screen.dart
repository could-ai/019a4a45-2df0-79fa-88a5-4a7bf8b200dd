import 'package:flutter/material.dart';

class FormulaSheetGeneratorScreen extends StatelessWidget {
  const FormulaSheetGeneratorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Formula Sheet Generator Screen - Coming Soon!', style: TextStyle(color: Colors.white)),
    );
  }
}
