import 'package:flutter/material.dart';

class TestGeneratorScreen extends StatelessWidget {
  const TestGeneratorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Test Generator Screen - Coming Soon!', style: TextStyle(color: Colors.white)),
    );
  }
}
