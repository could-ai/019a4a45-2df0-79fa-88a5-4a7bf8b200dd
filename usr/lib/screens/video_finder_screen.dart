import 'package:flutter/material.dart';

class VideoFinderScreen extends StatelessWidget {
  const VideoFinderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('One-Shot Video Finder Screen - Coming Soon!', style: TextStyle(color: Colors.white)),
    );
  }
}
